{
  "name": "html-preview",
  "displayName": "HTML Preview",
  "description": "Instantly preview HTML files inside the editor. One click, live reload on save, full asset support.",
  "version": "0.0.1",
  "publisher": "deyvisonsouto",
  "license": "MIT",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/deyvisonsouto/html-preview-extension.git"
  },
  "bugs": {
    "url": "https://github.com/deyvisonsouto/html-preview-extension/issues"
  },
  "homepage": "https://github.com/deyvisonsouto/html-preview-extension#readme",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "keywords": [
    "html",
    "preview",
    "live preview",
    "browser",
    "webview",
    "render",
    "static"
  ],
  "galleryBanner": {
    "color": "#E44D26",
    "theme": "dark"
  },
  "activationEvents": [
    "onLanguage:html"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "htmlPreview.open",
        "title": "Preview HTML",
        "icon": "$(open-preview)",
        "category": "HTML Preview"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "when": "resourceLangId == html",
          "command": "htmlPreview.open",
          "group": "navigation"
        }
      ],
      "commandPalette": [
        {
          "command": "htmlPreview.open",
          "when": "resourceLangId == html"
        }
      ]
    },
    "keybindings": [
      {
        "command": "htmlPreview.open",
        "key": "ctrl+shift+v",
        "mac": "cmd+shift+v",
        "when": "editorLangId == html"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^18.x",
    "@types/vscode": "^1.75.0",
    "typescript": "^5.3.0"
  }
}
